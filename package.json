{
  "name": "qubic",
  "version": "2.0.0-beta.12",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "tsc --watch",
    "publish": "lerna run publish",
    "build": "lerna run build"
  },
  "devDependencies": {
    "lerna": "^3.0.1",
    "prettier": "^1.14.2"
  },
  "dependencies": {
    "tslint": "^5.11.0",
    "tslint-eslint-rules": "^5.4.0"
  }
}
